---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chronosphere_drop_rule Resource - chronosphere"
subcategory: ""
description: |-
  Discards matching metric data at ingest time before it is stored. Supports unconditional drops, NaN-value drops, value-based drops, and conditional drops that activate only when a cardinality threshold is exceeded.
---

# chronosphere_drop_rule (Resource)

Discards matching metric data at ingest time before it is stored. Supports unconditional drops, NaN-value drops, value-based drops, and conditional drops that activate only when a cardinality threshold is exceeded.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the drop rule. Can be changed after creation.
- `query` (List of String) List of label filter queries that select which metrics to drop. A metric is dropped if it matches all filters in any one query.

### Optional

- `activated_drop_duration` (String) Once a conditional drop activates, how long it stays activated before re-checking against `rate_limit_threshold`.
- `active` (Boolean, Deprecated) Whether the drop rule is active. Deprecated: use `mode` instead.
- `conditional_drop` (Boolean) If `true`, the drop only activates when the configured `rate_limit_threshold` is exceeded.
- `drop_nan_value` (Boolean) If `true`, drops data points whose value is NaN, including any associated staleness markers.
- `mode` (String) Drop rule mode controlling whether it is enabled, disabled, or in a preview state. Defaults to `ENABLED`.
- `rate_limit_threshold` (Number) Percentage of the licensed metrics limit (0-100) at which a conditional drop activates.
- `slug` (String) Stable identifier for the drop rule. Generated from `name` if omitted. Immutable after creation.
- `value_based_drop` (Block List, Max: 1) Configuration for dropping data points whose value matches a target. (see [below for nested schema](#nestedblock--value_based_drop))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--value_based_drop"></a>
### Nested Schema for `value_based_drop`

Required:

- `target_drop_value` (Number) Data point value at which matching points are dropped.

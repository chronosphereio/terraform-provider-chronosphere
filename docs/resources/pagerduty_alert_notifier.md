---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chronosphere_pagerduty_alert_notifier Resource - chronosphere"
subcategory: ""
description: |-
  PagerDuty notifier that delivers monitor signals to PagerDuty as incidents via the Events API. Referenced from notification policies.
---

# chronosphere_pagerduty_alert_notifier (Resource)

PagerDuty notifier that delivers monitor signals to PagerDuty as incidents via the Events API. Referenced from notification policies.

## Example Usage

```terraform
resource "chronosphere_pagerduty_alert_notifier" "pagerduty" {
  name          = "PagerDuty Notifier"
  severity      = "info"
  url           = "https://events.pagerduty.com/v2/enqueue"
  routing_key   = "XXXXX"
  send_resolved = true

  details = {
    "runbook" = "http://runbook"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the notifier.
- `severity` (String) Severity of the incident. One of `critical`, `error`, `warning`, or `info`.
- `url` (String) PagerDuty API URL to send events to (e.g. `https://events.pagerduty.com/v2/enqueue`).

### Optional

- `basic_auth_password` (String, Sensitive) Password for HTTP basic auth when calling the PagerDuty API. Treat as a secret.
- `basic_auth_username` (String) Username for HTTP basic auth when calling the PagerDuty API. Mutually exclusive with `bearer_token`.
- `bearer_token` (String) Bearer token sent in the `Authorization` header when calling the PagerDuty API. Treat as a secret. Mutually exclusive with basic auth.
- `class` (String) Class of the event reported to PagerDuty (e.g. `cpu`, `database`). Supports Go templating.
- `client` (String) Name of the monitoring client identified in the notification.
- `client_url` (String) Backlink to the sender of the notification, shown in PagerDuty.
- `component` (String) Part or component of the affected system that is broken. Supports Go templating.
- `description` (String) Summary of the incident. Supports Go templating.
- `details` (Map of String) Arbitrary key/value pairs attached to the incident as additional context. Values support Go templating.
- `group` (String) Logical grouping of services the incident belongs to. Supports Go templating.
- `image` (Block List) Images attached to the PagerDuty incident. See https://developer.pagerduty.com/docs/events-api-v2/trigger-events/. (see [below for nested schema](#nestedblock--image))
- `link` (Block List) Hyperlinks attached to the PagerDuty incident. See https://developer.pagerduty.com/docs/events-api-v2/trigger-events/. (see [below for nested schema](#nestedblock--link))
- `proxy_url` (String, Deprecated) Deprecated and ignored. Custom proxy URLs are not supported.
- `routing_key` (String) PagerDuty integration key when using the `Events API v2` integration type. Treat as a secret. Mutually exclusive with `service_key`.
- `send_resolved` (Boolean) Whether to send a follow-up notification when an alert is resolved. Defaults to true.
- `service_key` (String, Sensitive) PagerDuty integration key when using the `Prometheus` integration type. Treat as a secret. Mutually exclusive with `routing_key`.
- `slug` (String) Stable identifier for the notifier. Generated from `name` if omitted. Immutable after creation.
- `tls_insecure_skip_verify` (Boolean) If true, skip TLS certificate verification when calling the PagerDuty API. Disable only in trusted environments.

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--image"></a>
### Nested Schema for `image`

Required:

- `src` (String) URL of the image to attach.

Optional:

- `alt` (String) Alternate text shown when the image cannot be rendered.
- `href` (String) Optional URL the image links to when clicked.


<a id="nestedblock--link"></a>
### Nested Schema for `link`

Required:

- `href` (String) URL the link points to.

Optional:

- `text` (String) Display text for the link.

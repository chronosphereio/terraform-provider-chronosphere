---
page_title: "chronosphere_log_control_config Resource - chronosphere"
subcategory: ""
description: |-
  Manages a Chronosphere Log Control Config.
---

# chronosphere_log_control_config (Resource)

Manages a Chronosphere Log Control Config.

## Example Usage

```terraform
resource "chronosphere_log_control_config" "example" {
  rules {
    name   = "Sample Debug Logs"
    mode   = "ENABLED"
    filter = "level = 'debug'"
    type   = "SAMPLE"
    sample {
      rate = 10
    }
  }

  rules {
    name   = "Drop Health Checks"
    mode   = "ENABLED"
    filter = "path = '/health'"
    type   = "DROP"
  }

  rules {
    name   = "Drop Sensitive Fields"
    mode   = "ENABLED"
    filter = "service = 'api'"
    type   = "DROP_FIELD"
    drop_field {
      field_regex = "password|token|secret"
    }
  }

  rules {
    name   = "Hash User IDs"
    mode   = "ENABLED"
    filter = "service = 'user-service'"
    type   = "REPLACE_FIELD"
    replace_field {
      field {
        selector = "user_id"
      }
      replace_regex = ".*"
      replace_all   = true
      replace_mode  = "HASH"
    }
  }

  rules {
    name   = "Emit Error Metrics"
    mode   = "ENABLED"
    filter = "level = 'error'"
    type   = "EMIT_METRICS"
    emit_metrics {
      name = "log_errors_total"
      mode = "COUNTER"
      counter {
        value {
          selector = "count"
        }
      }
      labels {
        key = "service"
        value {
          selector = "service"
        }
      }
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `rules` (Block List) (see [below for nested schema](#nestedblock--rules))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--rules"></a>
### Nested Schema for `rules`

Optional:

- `drop_field` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--drop_field))
- `emit_metrics` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics))
- `filter` (String)
- `mode` (String)
- `name` (String)
- `replace_field` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--replace_field))
- `sample` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--sample))
- `type` (String)

<a id="nestedblock--rules--drop_field"></a>
### Nested Schema for `rules.drop_field`

Optional:

- `field_regex` (String)
- `parent_path` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--drop_field--parent_path))

<a id="nestedblock--rules--drop_field--parent_path"></a>
### Nested Schema for `rules.drop_field.parent_path`

Optional:

- `selector` (String)



<a id="nestedblock--rules--emit_metrics"></a>
### Nested Schema for `rules.emit_metrics`

Optional:

- `counter` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--counter))
- `drop_log` (Boolean)
- `gauge` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--gauge))
- `histogram` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--histogram))
- `labels` (Block List) (see [below for nested schema](#nestedblock--rules--emit_metrics--labels))
- `mode` (String)
- `name` (String)

<a id="nestedblock--rules--emit_metrics--counter"></a>
### Nested Schema for `rules.emit_metrics.counter`

Optional:

- `value` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--counter--value))

<a id="nestedblock--rules--emit_metrics--counter--value"></a>
### Nested Schema for `rules.emit_metrics.counter.value`

Optional:

- `selector` (String)



<a id="nestedblock--rules--emit_metrics--gauge"></a>
### Nested Schema for `rules.emit_metrics.gauge`

Optional:

- `aggregation_type` (String)
- `value` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--gauge--value))

<a id="nestedblock--rules--emit_metrics--gauge--value"></a>
### Nested Schema for `rules.emit_metrics.gauge.value`

Optional:

- `selector` (String)



<a id="nestedblock--rules--emit_metrics--histogram"></a>
### Nested Schema for `rules.emit_metrics.histogram`

Optional:

- `value` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--histogram--value))

<a id="nestedblock--rules--emit_metrics--histogram--value"></a>
### Nested Schema for `rules.emit_metrics.histogram.value`

Optional:

- `selector` (String)



<a id="nestedblock--rules--emit_metrics--labels"></a>
### Nested Schema for `rules.emit_metrics.labels`

Optional:

- `key` (String)
- `value` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--emit_metrics--labels--value))

<a id="nestedblock--rules--emit_metrics--labels--value"></a>
### Nested Schema for `rules.emit_metrics.labels.value`

Optional:

- `selector` (String)




<a id="nestedblock--rules--replace_field"></a>
### Nested Schema for `rules.replace_field`

Optional:

- `field` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--replace_field--field))
- `mapped_value` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--replace_field--mapped_value))
- `replace_all` (Boolean)
- `replace_mode` (String)
- `replace_regex` (String)
- `static_value` (Block List, Max: 1) (see [below for nested schema](#nestedblock--rules--replace_field--static_value))

<a id="nestedblock--rules--replace_field--field"></a>
### Nested Schema for `rules.replace_field.field`

Optional:

- `selector` (String)


<a id="nestedblock--rules--replace_field--mapped_value"></a>
### Nested Schema for `rules.replace_field.mapped_value`

Optional:

- `default_value` (String)
- `pairs` (Block List) (see [below for nested schema](#nestedblock--rules--replace_field--mapped_value--pairs))
- `use_default` (Boolean)

<a id="nestedblock--rules--replace_field--mapped_value--pairs"></a>
### Nested Schema for `rules.replace_field.mapped_value.pairs`

Optional:

- `key` (String)
- `value` (String)



<a id="nestedblock--rules--replace_field--static_value"></a>
### Nested Schema for `rules.replace_field.static_value`

Optional:

- `value` (String)



<a id="nestedblock--rules--sample"></a>
### Nested Schema for `rules.sample`

Optional:

- `rate` (Number)

## Import

Import is supported using the following syntax:

```shell
# Replace 'example-id' with the actual resource ID
terraform import chronosphere_log_control_config.example example-id
```

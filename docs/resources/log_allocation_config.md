---
page_title: "chronosphere_log_allocation_config Resource - chronosphere"
subcategory: ""
description: |-
  Manages a Chronosphere Log Allocation Config.
---

# chronosphere_log_allocation_config (Resource)

Manages a Chronosphere Log Allocation Config.

## Example Usage

```terraform
resource "chronosphere_log_allocation_config" "example" {
  default_dataset {
    allocation {
      percent_of_license = 90.0
    }
    priorities {
      high_priority_filter {
        query = "severity='error'"
      }
      low_priority_filter {
        query = "severity='debug'"
      }
    }
  }

  dataset_allocation {
    dataset_id = chronosphere_dataset.custom_logs.id
    allocation {
      percent_of_license = 10.0
    }
    priorities {
      high_priority_filter {
        query = "severity='error'"
      }
      low_priority_filter {
        query = "severity='info'"
      }
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `default_dataset` (Block List, Min: 1, Max: 1) (see [below for nested schema](#nestedblock--default_dataset))

### Optional

- `dataset_allocation` (Block List, Max: 10) (see [below for nested schema](#nestedblock--dataset_allocation))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--default_dataset"></a>
### Nested Schema for `default_dataset`

Required:

- `allocation` (Block List, Min: 1, Max: 1) (see [below for nested schema](#nestedblock--default_dataset--allocation))

Optional:

- `priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_dataset--priorities))

<a id="nestedblock--default_dataset--allocation"></a>
### Nested Schema for `default_dataset.allocation`

Required:

- `percent_of_license` (Number)


<a id="nestedblock--default_dataset--priorities"></a>
### Nested Schema for `default_dataset.priorities`

Optional:

- `high_priority_filter` (Block List) (see [below for nested schema](#nestedblock--default_dataset--priorities--high_priority_filter))
- `low_priority_filter` (Block List) (see [below for nested schema](#nestedblock--default_dataset--priorities--low_priority_filter))

<a id="nestedblock--default_dataset--priorities--high_priority_filter"></a>
### Nested Schema for `default_dataset.priorities.high_priority_filter`

Required:

- `query` (String)


<a id="nestedblock--default_dataset--priorities--low_priority_filter"></a>
### Nested Schema for `default_dataset.priorities.low_priority_filter`

Required:

- `query` (String)




<a id="nestedblock--dataset_allocation"></a>
### Nested Schema for `dataset_allocation`

Required:

- `allocation` (Block List, Min: 1, Max: 1) (see [below for nested schema](#nestedblock--dataset_allocation--allocation))
- `dataset_id` (String)

Optional:

- `priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--dataset_allocation--priorities))

<a id="nestedblock--dataset_allocation--allocation"></a>
### Nested Schema for `dataset_allocation.allocation`

Required:

- `percent_of_license` (Number)


<a id="nestedblock--dataset_allocation--priorities"></a>
### Nested Schema for `dataset_allocation.priorities`

Optional:

- `high_priority_filter` (Block List) (see [below for nested schema](#nestedblock--dataset_allocation--priorities--high_priority_filter))
- `low_priority_filter` (Block List) (see [below for nested schema](#nestedblock--dataset_allocation--priorities--low_priority_filter))

<a id="nestedblock--dataset_allocation--priorities--high_priority_filter"></a>
### Nested Schema for `dataset_allocation.priorities.high_priority_filter`

Required:

- `query` (String)


<a id="nestedblock--dataset_allocation--priorities--low_priority_filter"></a>
### Nested Schema for `dataset_allocation.priorities.low_priority_filter`

Required:

- `query` (String)

## Import

Import is supported using the following syntax:

```shell
# Replace 'example-id' with the actual resource ID
terraform import chronosphere_log_allocation_config.example example-id
```

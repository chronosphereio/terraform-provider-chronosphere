---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chronosphere_recording_rule Resource - chronosphere"
subcategory: ""
description: |-
  Evaluates a PromQL expression at a fixed interval and writes the result to a new time series. Useful for precomputing expensive queries or producing derived metrics.
---

# chronosphere_recording_rule (Resource)

Evaluates a PromQL expression at a fixed interval and writes the result to a new time series. Useful for precomputing expensive queries or producing derived metrics.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `expr` (String) PromQL expression evaluated at each interval. The result is written to a new series named by `metric_name` (or `name` if unset).
- `name` (String) Display name of the recording rule. Can be changed after creation.

### Optional

- `bucket_id` (String) ID of the bucket the recording rule belongs to. At least one of `bucket_id` or `execution_group` must be set; if both are set their values must match.
- `execution_group` (String) Slug of the execution group in which the rule is evaluated. Rules in the same group run sequentially at the configured interval; all rules in a group must finish before the next iteration starts. At least one of `bucket_id` or `execution_group` must be set.
- `execution_mode` (String) Execution mode controlling whether the recording rule is active.
- `interval` (String) Evaluation interval (e.g. `30s`, `1m`). Defaults to `60s` when unset.
- `labels` (Map of String) Key/value labels added to every series produced by this recording rule.
- `metric_name` (String) Name of the output time series produced by `expr`. Must be a valid metric name. Defaults to `name` if omitted.
- `slug` (String) Stable identifier for the recording rule. Generated from `name` if omitted. Immutable after creation.

### Read-Only

- `id` (String) The ID of this resource.

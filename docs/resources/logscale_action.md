---
page_title: "chronosphere_logscale_action Resource - chronosphere"
subcategory: ""
description: |-
  Manages a Chronosphere Logscale Action.
---

# chronosphere_logscale_action (Resource)

Manages a Chronosphere Logscale Action.

## Example Usage

```terraform
resource "chronosphere_logscale_action" "email_action" {
  repository = "default"
  name       = "Email Alert Action"

  email_action {
    recipients       = ["oncall@example.com"]
    subject_template = "Alert: {{.Query.Name}}"
    body_template    = "Alert triggered at {{.Time}}: {{.Query.QueryString}}"
    attach_csv       = true
    use_proxy        = false
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `repository` (String)

### Optional

- `email_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--email_action))
- `humio_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--humio_action))
- `ops_genie_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--ops_genie_action))
- `pager_duty_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pager_duty_action))
- `slack_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--slack_action))
- `slack_post_message_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--slack_post_message_action))
- `slug` (String)
- `upload_file_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--upload_file_action))
- `victor_ops_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--victor_ops_action))
- `webhook_action` (Block List, Max: 1) (see [below for nested schema](#nestedblock--webhook_action))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--email_action"></a>
### Nested Schema for `email_action`

Required:

- `recipients` (List of String)

Optional:

- `attach_csv` (Boolean)
- `body_template` (String)
- `subject_template` (String)
- `use_proxy` (Boolean)


<a id="nestedblock--humio_action"></a>
### Nested Schema for `humio_action`

Required:

- `ingest_token` (String)


<a id="nestedblock--ops_genie_action"></a>
### Nested Schema for `ops_genie_action`

Required:

- `api_url` (String)
- `ops_genie_key` (String)

Optional:

- `use_proxy` (Boolean)


<a id="nestedblock--pager_duty_action"></a>
### Nested Schema for `pager_duty_action`

Required:

- `routing_key` (String)
- `severity` (String)

Optional:

- `use_proxy` (Boolean)


<a id="nestedblock--slack_action"></a>
### Nested Schema for `slack_action`

Required:

- `url` (String)

Optional:

- `fields` (Map of String)
- `use_proxy` (Boolean)


<a id="nestedblock--slack_post_message_action"></a>
### Nested Schema for `slack_post_message_action`

Required:

- `api_token` (String)
- `channels` (List of String)

Optional:

- `fields` (Map of String)
- `use_proxy` (Boolean)


<a id="nestedblock--upload_file_action"></a>
### Nested Schema for `upload_file_action`

Required:

- `file_name` (String)


<a id="nestedblock--victor_ops_action"></a>
### Nested Schema for `victor_ops_action`

Required:

- `message_type` (String)
- `notify_url` (String)

Optional:

- `use_proxy` (Boolean)


<a id="nestedblock--webhook_action"></a>
### Nested Schema for `webhook_action`

Required:

- `method` (String)
- `url` (String)

Optional:

- `body_template` (String)
- `headers` (Map of String)
- `ignore_ssl` (Boolean)
- `use_proxy` (Boolean)

## Import

Import is supported using the following syntax:

```shell
# Replace 'example-id' with the actual resource ID
terraform import chronosphere_logscale_action.example example-id
```

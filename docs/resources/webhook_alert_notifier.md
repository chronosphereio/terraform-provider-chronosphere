---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "chronosphere_webhook_alert_notifier Resource - chronosphere"
subcategory: ""
description: |-
  Webhook notifier that POSTs monitor signal payloads to an arbitrary HTTP endpoint. Referenced from notification policies.
---

# chronosphere_webhook_alert_notifier (Resource)

Webhook notifier that POSTs monitor signal payloads to an arbitrary HTTP endpoint. Referenced from notification policies.

## Example Usage

```terraform
resource "chronosphere_webhook_alert_notifier" "webhook" {
  name          = "Webhook"
  url           = "http://example.com/url"
  send_resolved = false
  bearer_token  = "bearer-token"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name of the notifier.
- `url` (String) Webhook URL that receives the alert payload via HTTP POST.

### Optional

- `basic_auth_password` (String, Sensitive) Password for HTTP basic auth when calling the webhook. Treat as a secret.
- `basic_auth_username` (String) Username for HTTP basic auth when calling the webhook. Mutually exclusive with `bearer_token`.
- `bearer_token` (String) Bearer token sent in the `Authorization` header when calling the webhook. Treat as a secret. Mutually exclusive with basic auth.
- `proxy_url` (String, Deprecated) Deprecated and ignored. Custom proxy URLs are not supported.
- `send_resolved` (Boolean) Whether to send a follow-up notification when an alert is resolved. Defaults to true.
- `slug` (String) Stable identifier for the notifier. Generated from `name` if omitted. Immutable after creation.
- `tls_insecure_skip_verify` (Boolean) If true, skip TLS certificate verification when calling the webhook. Disable only in trusted environments.

### Read-Only

- `id` (String) The ID of this resource.

---
page_title: "chronosphere_resource_pools_config Resource - chronosphere"
subcategory: ""
description: |-
  Manages a Chronosphere Resource Pools Config.
---

# chronosphere_resource_pools_config (Resource)

Manages a Chronosphere Resource Pools Config.

## Example Usage

```terraform
resource "chronosphere_resource_pools_config" "example" {
  default_pool {
    allocation {
      percent_of_license = 70
    }
    priorities {
      high_priority_match_rules {
        name   = "Critical Services"
        filter = "service:~(api|checkout|payment)"
      }
      low_priority_match_rules {
        name   = "Development"
        filter = "env:~(dev|test)"
      }
    }
  }

  pool {
    name = "Team A Pool"
    allocation {
      percent_of_license = 20
    }
    match_rules {
      name   = "Team A Services"
      filter = "team:a"
    }
    priorities {
      high_priority_match_rules {
        name   = "Team A Production"
        filter = "env:prod"
      }
    }
  }

  pool {
    name = "Team B Pool"
    allocation {
      percent_of_license = 10
    }
    match_rules {
      name   = "Team B Services"
      filter = "team:b"
    }
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `default_pool` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool))
- `pool` (Block List, Max: 128) (see [below for nested schema](#nestedblock--pool))
- `pools` (Block List, Max: 128, Deprecated) (see [below for nested schema](#nestedblock--pools))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--default_pool"></a>
### Nested Schema for `default_pool`

Optional:

- `allocation` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--allocation))
- `priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--priorities))
- `priority_thresholds` (Block List) (see [below for nested schema](#nestedblock--default_pool--priority_thresholds))

<a id="nestedblock--default_pool--allocation"></a>
### Nested Schema for `default_pool.allocation`

Optional:

- `fixed_value` (Block List) (see [below for nested schema](#nestedblock--default_pool--allocation--fixed_value))
- `percent_of_license` (Number)
- `priority_thresholds` (Block List) (see [below for nested schema](#nestedblock--default_pool--allocation--priority_thresholds))

<a id="nestedblock--default_pool--allocation--fixed_value"></a>
### Nested Schema for `default_pool.allocation.fixed_value`

Required:

- `license` (String)
- `value` (Number)


<a id="nestedblock--default_pool--allocation--priority_thresholds"></a>
### Nested Schema for `default_pool.allocation.priority_thresholds`

Required:

- `license` (String)

Optional:

- `all_priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--allocation--priority_thresholds--all_priorities))
- `default_and_low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--allocation--priority_thresholds--default_and_low_priority))
- `low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--allocation--priority_thresholds--low_priority))

<a id="nestedblock--default_pool--allocation--priority_thresholds--all_priorities"></a>
### Nested Schema for `default_pool.allocation.priority_thresholds.all_priorities`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--default_pool--allocation--priority_thresholds--default_and_low_priority"></a>
### Nested Schema for `default_pool.allocation.priority_thresholds.default_and_low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--default_pool--allocation--priority_thresholds--low_priority"></a>
### Nested Schema for `default_pool.allocation.priority_thresholds.low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)




<a id="nestedblock--default_pool--priorities"></a>
### Nested Schema for `default_pool.priorities`

Optional:

- `high_priority_match_rules` (List of String)
- `low_priority_match_rules` (List of String)


<a id="nestedblock--default_pool--priority_thresholds"></a>
### Nested Schema for `default_pool.priority_thresholds`

Required:

- `license` (String)

Optional:

- `all_priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--priority_thresholds--all_priorities))
- `default_and_low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--priority_thresholds--default_and_low_priority))
- `low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--default_pool--priority_thresholds--low_priority))

<a id="nestedblock--default_pool--priority_thresholds--all_priorities"></a>
### Nested Schema for `default_pool.priority_thresholds.all_priorities`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--default_pool--priority_thresholds--default_and_low_priority"></a>
### Nested Schema for `default_pool.priority_thresholds.default_and_low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--default_pool--priority_thresholds--low_priority"></a>
### Nested Schema for `default_pool.priority_thresholds.low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)




<a id="nestedblock--pool"></a>
### Nested Schema for `pool`

Required:

- `name` (String)

Optional:

- `allocation` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pool--allocation))
- `match_rule` (String, Deprecated)
- `match_rules` (List of String)
- `priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pool--priorities))

<a id="nestedblock--pool--allocation"></a>
### Nested Schema for `pool.allocation`

Optional:

- `fixed_value` (Block List) (see [below for nested schema](#nestedblock--pool--allocation--fixed_value))
- `percent_of_license` (Number)
- `priority_thresholds` (Block List) (see [below for nested schema](#nestedblock--pool--allocation--priority_thresholds))

<a id="nestedblock--pool--allocation--fixed_value"></a>
### Nested Schema for `pool.allocation.fixed_value`

Required:

- `license` (String)
- `value` (Number)


<a id="nestedblock--pool--allocation--priority_thresholds"></a>
### Nested Schema for `pool.allocation.priority_thresholds`

Required:

- `license` (String)

Optional:

- `all_priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pool--allocation--priority_thresholds--all_priorities))
- `default_and_low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pool--allocation--priority_thresholds--default_and_low_priority))
- `low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pool--allocation--priority_thresholds--low_priority))

<a id="nestedblock--pool--allocation--priority_thresholds--all_priorities"></a>
### Nested Schema for `pool.allocation.priority_thresholds.all_priorities`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--pool--allocation--priority_thresholds--default_and_low_priority"></a>
### Nested Schema for `pool.allocation.priority_thresholds.default_and_low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--pool--allocation--priority_thresholds--low_priority"></a>
### Nested Schema for `pool.allocation.priority_thresholds.low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)




<a id="nestedblock--pool--priorities"></a>
### Nested Schema for `pool.priorities`

Optional:

- `high_priority_match_rules` (List of String)
- `low_priority_match_rules` (List of String)



<a id="nestedblock--pools"></a>
### Nested Schema for `pools`

Required:

- `name` (String)

Optional:

- `allocation` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pools--allocation))
- `match_rule` (String, Deprecated)
- `match_rules` (List of String)
- `priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pools--priorities))

<a id="nestedblock--pools--allocation"></a>
### Nested Schema for `pools.allocation`

Optional:

- `fixed_value` (Block List) (see [below for nested schema](#nestedblock--pools--allocation--fixed_value))
- `percent_of_license` (Number)
- `priority_thresholds` (Block List) (see [below for nested schema](#nestedblock--pools--allocation--priority_thresholds))

<a id="nestedblock--pools--allocation--fixed_value"></a>
### Nested Schema for `pools.allocation.fixed_value`

Required:

- `license` (String)
- `value` (Number)


<a id="nestedblock--pools--allocation--priority_thresholds"></a>
### Nested Schema for `pools.allocation.priority_thresholds`

Required:

- `license` (String)

Optional:

- `all_priorities` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pools--allocation--priority_thresholds--all_priorities))
- `default_and_low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pools--allocation--priority_thresholds--default_and_low_priority))
- `low_priority` (Block List, Max: 1) (see [below for nested schema](#nestedblock--pools--allocation--priority_thresholds--low_priority))

<a id="nestedblock--pools--allocation--priority_thresholds--all_priorities"></a>
### Nested Schema for `pools.allocation.priority_thresholds.all_priorities`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--pools--allocation--priority_thresholds--default_and_low_priority"></a>
### Nested Schema for `pools.allocation.priority_thresholds.default_and_low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)


<a id="nestedblock--pools--allocation--priority_thresholds--low_priority"></a>
### Nested Schema for `pools.allocation.priority_thresholds.low_priority`

Optional:

- `fixed_value` (Number)
- `percent_of_pool_allocation` (Number)




<a id="nestedblock--pools--priorities"></a>
### Nested Schema for `pools.priorities`

Optional:

- `high_priority_match_rules` (List of String)
- `low_priority_match_rules` (List of String)

## Import

Import is supported using the following syntax:

```shell
# Replace 'example-id' with the actual resource ID
terraform import chronosphere_resource_pools_config.example example-id
```
